# 남은 생성 대기 목록 (2026-07-28 갱신)

**A~D 섹션은 전부 끝났다.** 2026-07-27 판의 A 배경 / B 동물 / C 사물 / D 구도·효과는
모두 생성·배선·검증까지 마쳤다. 이 문서는 그 이후 상태만 남긴다.

현재 팩 **9,195키 / 52.7 MB**. 슬롯 13개 전부 빈 칸 0.

| 슬롯 | 축 | 태그 |
|---|---|---|
| 머리 · 눈얼굴 · 표정 · 신체 · 종족 | 24 | 1,552 |
| 의상 | 12 | 1,199 |
| 소품·장식 | 13 | 1,201 |
| 자세 (1인) | 19 | 1,577 |
| 다인원 자세 | 13 | 438 |
| 배경 | 9 | 496 |
| 사물 (캐릭터 + 씬 공용) | 9 | 1,954 |
| 동물·식물 | 6 | 272 |
| 효과·기호 (조명 포함) | 4 | 496 |

한도는 2026-07-28 06:00 KST 에 4차 초기화했다. 예산 **3,500** / 사용 **2,379**.
운영 규칙은 `wildcards/thumb/_APPROVAL.md` 가 SSOT — 한도 06:00 초기화, 작업 07:00
재개, **Rate Limit 에러를 받으면 그날 작업 종료**.

---

## A. 성인 축 — 242 · **도감만 만들어 뒀다**

사용자 결정(2026-07-28): "성인 컨텐츠는 어차피 위험이 있어 도감 분류만 해놓고
와일드카드만 만들어주세요."

`tools/build_nsfw_catalog.py` 가 `wildcards/nsfw/` 에 8개 분류로 정리해 뒀다
(체액 23 · 성기 43 · 음모 9 · 유두 18 · 가슴 49 · 둔부 18 · 구속 9 · 노출의상 77).
**썸네일은 만들지 않는다** — 벤치 템플릿이 `young female` 로 고정돼 있고,
그 베이스로 성적인 이미지를 만들지 않는다는 것이 생성하지 않는 이유다.
UI 배선도 뺐다(빈 그리드였다).

사용자가 검토 중인 대안: `head out of frame` / `faceless female` 로 인물 특징을
지운 템플릿. 그 경우에도 **네거티브의 `{adolescent, mature female}` 를 먼저 바꿔야
한다** — 지금 그대로면 어려 보이는 쪽으로 밀린다.

---

## B. 검수 미실시 축

자세 32축·의상 25축·사물 9축 중 상당수는 컨택트 시트를 아직 안 봤다.
보면 시드 재시도 후보가 나온다. 알려진 것:

- **하의 8개 프레이밍** — `pants` 계열 7 + `long skirt` 이 허벅지에서 잘려 서로
  구분이 안 된다. 하반신 프레이밍으로 8장.
- **`pose_display` 잔여 2** — `shaking head` / `hobble` 이 여전히 약하다.
- `forest`(원경으로 나옴) · `classroom`(하늘이 조금 돌아옴).

---

## C. 썸네일이 없는 채로 남긴 것 (의도)

섹션 전수 조사(2026-07-28)에서 확인한 것들. **트리로만 접근한다.**

- **구도(composition)** 292개 — 3축 콤보 프리셋이 주 경로고, 트리가 나머지를 맡는다.
  썸네일을 만들려면 축마다 프레이밍이 달라져 설계가 따로 필요하다.
- **기타·텍스트(meta 잔여)** 약 880개 — `comic`/`border`/`4koma` 같은 화면 형식과
  `artist name`/`signature` 같은 관리용 태그. 대부분 그림 요소가 아니다.
- **freq < 149 전체** — 절단선 아래는 한글 설명도 거의 없다. 아는 사람이 직접
  입력하는 영역이고, 슬롯 입력창의 자동완성이 그 경로다.

---

## 이 프로젝트에서 반복 확인된 것 (새 섹션 착수 전 읽을 것)

1. **파일럿 먼저.** 축 단위 프레이밍 실패를 6~25장으로 잡는다. 배경에서 25장이
   295장의 방향을 정했고, 특징 슬롯에서 27장이 수백 장을 살렸다.
2. **한 축에 두 스케일이 있으면 반드시 갈린다.** 축 이름에 `·` 로 두 부위가 붙어
   있으면 신호다 — 팔·다리, 얼굴·몸에 손, 소형 장신구, 장소·건물 네 번 모두 그랬다.
3. **태그가 화면을 못 채우면 NAI 는 자기 기본값으로 메운다.** 그 기본값은 맥락마다
   다르다(실내=음식 정물, 실외=뭉게구름). 맥락 태그로는 안 잡히고 `-1::` 직접 상쇄만
   듣는다. 그리고 하나를 누르면 다른 하나가 들어오므로 **둘 다 눌러야** 한다.
4. **목록을 두 군데에 손으로 적으면 갈라진다.** 이번 프로젝트 사고의 대부분이 이것이다.
   파생시키고 assert 로 막는다. 판단이 필요해 목록이 불가피하면 최소한 assert 는 건다.
5. **192px 컨택트 시트로 판정하지 말 것.** 축소본에서는 안경·눈매 같은 작은 신호가
   사라진다 — `jimiko`/`mesugaki` 를 "렌더 실패" 로 잘못 판정했다가 원본에서 뒤집었다.
6. **재생성본이 원본보다 나쁠 수 있다.** 시드만 바꿔 재시도하되 눈으로 보고 선별 병합한다.
7. **배치를 겹쳐 돌리지 말 것.** `429 Concurrent generation is locked` 가 난다.
   이것은 할당량 Rate Limit 이 아니다 — 원장에 구분해 적었다.
8. **`_todo/` 에는 미생성분만 적는다.** 축 파일 전체를 적으면 이미 만든 것까지 큐에
   올라 예산을 먹고 뒤쪽 축이 통째로 잘린다(실측 1회). 생성기가 팩과 대조해 쓴다.
9. **제외 목록도 '배정된 것'이다.** 생성기들이 `_` 접두 파일을 건너뛰는 규칙 때문에
   의도적 제외 목록(`_relational_meta.txt`)까지 무시돼, 빼 둔 태그가 축으로
   되돌아왔다(실측 2건). 중간 산출물과 제외 목록은 성격이 다르다.
10. **UI 에서 뭔가를 '남겨 두는' 이유는 주석에 적고, 그 이유가 사라지면 뗀다.**
    소품 탐색기 주석에 "동물 축이 생기면 뗀다"고 적어 뒀고, 축이 생긴 뒤 조사에서
    그 줄이 판정 근거가 됐다. 안 적었으면 근거 없이 계속 남았을 것이다.
